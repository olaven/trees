type Query {

    plant(id: Int): Plant,
    plants: [Plant!],

    location(id: Int): Location,
    locations: [Location!]
}

#type Mutation {
#
#        # TODO
#}


type Location {
    id: Int!,
    x: Float!,
    y: Float!
}

type Plant {
    id: Int!
    name: String!,
    description: String!,
    height: Float!,
    age: Int!,
    location: Location!,
}