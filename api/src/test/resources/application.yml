spring:
  datasource:
    url: "jdbc:tc:postgresql://postgres_movies:5432/postgres"
    username: "docker"
    password: "docker"
    driver-class-name: "org.testcontainers.jdbc.ContainerDatabaseDriver"

  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.spatial.dialect.postgis.PostgisDialect
    hibernate:
      ddl-auto: create-drop # TODO Should be validate (with flyway or similar) at some point

eureka:
  client:
    enabled: false